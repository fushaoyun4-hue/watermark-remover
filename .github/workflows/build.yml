name: Build APK

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: temurin

      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Create local.properties
        run: echo "sdk.dir=$ANDROID_HOME" > local.properties

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: List Gradle projects & APK tasks
        run: |
          echo "=== Available projects ==="
          ./gradlew projects --no-daemon 2>&1 | head -20
          echo "=== APK-related tasks ==="
          ./gradlew tasks --group=build --no-daemon 2>&1 | grep -i apk || echo "No apk tasks found"

      - name: Build Debug APK
        id: build_step
        run: |
          ./gradlew clean assembleDebug --no-daemon --info 2>&1 | tee build.log | tail -100
          echo "=== Exit code: $? ==="

      - name: Capture build error
        if: failure()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          mkdir -p ci_artifacts
          cp build.log ci_artifacts/ci_error.txt || true
          echo "=== APK debug dir ===" >> ci_artifacts/ci_error.txt
          ls -laR app/build/outputs/apk/debug/ >> ci_artifacts/ci_error.txt 2>&1 || true
          echo "=== All APK files ===" >> ci_artifacts/ci_error.txt
          find app/build -name "*.apk" >> ci_artifacts/ci_error.txt 2>&1 || true
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git"
          git add ci_artifacts/ci_error.txt 2>/dev/null || true
          git commit -m "ci: capture build error [skip ci]" 2>&1 | tail -2 || true
          git push origin HEAD:ci-debug --force 2>&1 | tail -2 || true

      - name: Diagnose build output
        run: |
          echo "=== Build log last 30 lines ==="
          tail -30 build.log
          echo "=== Full app/build directory tree ==="
          find app/build -type f 2>&1 | head -100
          echo "=== app/build/outputs directory ==="
          ls -laR app/build/outputs/ 2>&1 || echo "Directory NOT found"
          echo "=== Find ALL .apk in entire project ==="
          find . -name "*.apk" -type f 2>&1
          echo "=== APK count ==="
          find . -name "*.apk" -type f | wc -l

      - name: Upload Debug APKs
        if: steps.build_step.conclusion == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: debug-apks-${{ github.run_id }}
          path: |
            app/build/outputs/apk/debug/
            app/build/outputs/apk/release/
            **/*.apk
          if-no-files-found: warn

      - name: Upload Debug APKs
        if: steps.build_step.conclusion == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: debug-apks-${{ github.run_id }}
          path: app/build/outputs/apk/debug/

      - name: Publish to GitHub Release
        if: steps.build_step.conclusion == 'success'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: debug-${{ github.sha }}
          name: Debug APK (${{ github.sha }})
          body: |
            Built from `${{ github.sha }}`
          draft: false
          prerelease: true
          files: |
            app/build/outputs/apk/debug/*.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
